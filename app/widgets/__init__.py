"""Reusable presentation components and the application shell.

Holds shared, business-agnostic UI building blocks (Part 2: *never duplicate UI*). The
:class:`~app.widgets.main_window.MainWindow` is the shell that hosts module views as they are
implemented; module-specific widgets live under each module's ``ui/`` package.
"""
