"""Members module — gym member profiles and management.

A vertical slice (model, repository, dtos, validators, events, service, ui) for creating,
searching and listing members. Depends on the security module only for authorization
(permissions) at the UI/controller boundary.
"""
