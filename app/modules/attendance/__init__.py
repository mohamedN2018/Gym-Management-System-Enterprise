"""Attendance module — member check-in (access control).

Records member check-ins and decides entry by verifying an active subscription. This is the
foundation for QR/turnstile access; the current UI uses manual membership-number entry.
"""
