"""Business modules.

Each module is an isolated vertical slice (models, repositories, services, controllers,
validators, dtos, events, permissions, ui, tests, docs). Modules communicate only through
shared services or the event bus — never by importing another module's internals (Part 2).
"""
